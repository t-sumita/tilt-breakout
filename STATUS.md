# STATUS.md — 作業状況

## 現在のバージョン
`0.7.1`

## 最終更新
2026-08-23

## 状態
- [x] リポジトリ初期化・GitHub リポジトリ作成・初回コミット・push
- [x] コアゲーム実装(config/mathutils/ball/physics/stages/input/renderer/game/main)
- [x] サイト共通部品の配線(バッジ/フッター/manifest/favicon/Cloudflare Analytics プレースホルダ)
- [x] 3点セット(CLAUDE.md/STATUS.md/PLAN.md)作成
- [x] `.claude/settings.json` 作成(acceptEdits + git push 系拒否)
- [x] ローカルサーバー+Playwright での動作確認(タイトル→発射→プレイ→パドル反射→スコア加算まで確認、エラーなし)
- [x] GitHub Pages 公開(`https://tilt-breakout.subutomo.dev/`、`CNAME` 追加済み)
- [x] favicon/apple-touch-icon を本番デザインへ差し替え
- [x] パドル移動速度を3倍化
- [x] ステージ2の回転十字ジャマーの中央正方形からの距離を玉1個分拡大
- [x] favicon を Subutomo エコシステム共通の正本デザイン(オレンジ背景+白円+「す」)へ差し戻し
- [x] ステージ1・3列目の左右最外側の破壊不能ジャマーをハードブロック(3ヒット)化
- [x] ステージ2の凹ブラケットの脚を短縮し、中央穴の高さを玉1個分拡大(30px→42px)
- [x] 玉の速度ベクトルが水平から5度未満にならないよう全反射箇所で角度を補正
- [x] ステージ3中央コアの初回ヒット時ランダムHPを2桁(10〜99)から1桁(1〜9)に変更
- [x] 玉の物理を重力+空気抵抗モデルへ刷新(`js/physics.js`)。Node シミュレーションで3ステージすべての到達可能性を検証済み
- [x] パドルの「受け」動作(内積判定による反発係数の切り替え)を実装
- [x] 玉のキャッチ・パドル追従・再発射(発射操作の統一)を実装
- [x] Config画面(パドル速度/重力/空気抵抗/制限時間/ボール数/キャッチしきい値/ステージ選択)を実装。開いている間はゲーム・タイマーを一時停止
- [x] オールクリア(1→2→3通しクリア)判定と花火演出、ステージ選択でジャンプした場合の対象外扱いを実装
- [x] HUDにハイスコア(localStorage永続化)・残り時間を常時表示。`SUBUTOMO_BADGE_CONFIG.privacy.localStorage` を `true` に変更
- [x] ステージ2下段2列の通常/ジャマー混在ブロックを各段8列→6列に削減し、プレイフィールド中央へ再配置(チェッカー柄反転ルールは維持)
- [x] ステージ3の回転リング3枚を「穴(空き角度)1.5倍・最内周がコア外周に接触・リング間隔1.5倍」の幾何に再計算し、Node シミュレーション+Playwright ヘッドレスブラウザでクリア可能性を再検証
- [x] Config画面に「規定値に戻す」ボタンを追加し、各パラメータのラベルに常に既定値を併記(例: `120 (既定値: 120)`)
- [x] 規定値を空気抵抗0.2→0.4・キャッチ速度しきい値110→100に変更
- [x] ステージ2下段2段を左右3列ずつ(各段6個)に分割し中央に1ブロック分の空きを作成。右側3列はブロック/ジャマーの位置を入れ替え
- [x] Config画面に隠しコマンド(メンテナンスボタン→合言葉「はだかのブタがいる」→「subuta」)を追加。正答で既存のオールクリア花火演出のみを再現(実際のオールクリア記録・スコア・ハイスコアには一切影響しない)
- [x] README.md を新規作成(絶望要塞へのリスペクト・全仕様・隠しコマンドのさりげない記載を含む)
- [x] タイトル画面で60秒間無操作が続くと自動でデモプレイ(アトラクトモード)を開始する仕組みを実装。ランダムな1ステージを簡易AIで自動プレイし、スコア/ハイスコアには反映せず、何らかの操作で即座に終了

## 直近の変更
- `0.7.1`: `0.7.0` で追加した3機能への修正3件。(1) メンテナンスボタンの常時可視化: `style.css` の `.config-maintenance` が既定状態で `opacity: 0.55` と暗い文字色 `#3a4a58` の組み合わせになっており、パネル背景(`--panel: #0f1630`)に対して事実上視認できていなかった(`:hover` 時のみ `opacity: 1` で見えていた)。opacity切り替えを廃し、常時視認可能な固定色 `#506a80`(ホバー時のみ `#7d94a8` に明るくする)へ変更。(2) 花火の演出時間: 隠しコマンドの花火演出(`playEasterEggDemo()`)が固定6秒(`EASTER_EGG_DURATION_SEC`)で自動終了していたのを、本番のオールクリア演出(`state==='win'`)と同じく時間で終わらせず、タップ等の操作(既存の `handleTap()` 内 `easterEgg.active` 分岐)があるまで継続するよう変更(`Game.easterEgg` から `timer` フィールドを削除)。あわせて花火の打ち上げ頻度を密度不足(「短くて寂しい」)の指摘を受けて `Math.random() < dt * 1.5` → `Math.random() < dt * FIREWORKS_BURST_RATE`(`FIREWORKS_BURST_RATE = 3.2`)へ引き上げ、隠しコマンド演出・本番オールクリア演出の両方に共通適用した。(3) デモプレイの縦移動・傾き: `_updateDemo()` のAIがボールのx座標を追うだけで縦方向には全く動いていなかったのを、待機中は下限付近でゆるやかな正弦波の上下揺れ(`idleY`)、ボールが水平方向に近く(`paddle.halfW*3` 以内)かつ落下中(`ball.vy > 0`)のときはボールへ迎えに上がる(`clamp(ball.y - 36, ...)`)よう追加し、`paddle.vy` を実速度から算出。傾き(`paddle.tilt`)は横移動速度に比例させ(`DEMO_TILT_GAIN = PADDLE.maxTiltDeg / DEMO_PADDLE_FOLLOW_SPEED`)、最大追従速度で最大傾き角になるようスケーリングした。`physics.js` の `collidePaddle()` は元々 `paddle.vy`/`paddle.tilt` をキャッチ判定・反射角計算に使う実装だったため、この変更は見た目だけでなく実際の当たり判定にも反映される。Node シミュレーション(イースターエッグが10秒経過後も `active` のままでタップ後に終了すること、デモパドルの `y`/`tilt` が可動域・最大傾き角の範囲内に収まりつつ変動すること)と Playwright ヘッドレスブラウザ(Configパネルのメンテナンスボタンがホバーなしで視認できるスクリーンショット、実際に61秒待って発生させたデモプレイでパドルが上下・傾きともに変化する複数フレームのスクリーンショット)の両方で確認済み(検証用一時スクリプト・スクリーンショットはコミット前に削除)。
- `0.7.0`: 3つの新機能を追加。(1) 隠しコマンド: Config画面(`index.html`)に目立たない `#config-maintenance` ボタン(9pxの暗い文字色・枠線なし)を追加し、クリックすると `window.prompt('はだかのブタがいる')` が表示される。ローマ字「subuta」(大文字小文字区別なし)を入力すると `Game.playEasterEggDemo()` が呼ばれ、既存の花火+`CONGRATULATIONS!`演出だけを再現する。実装は `Game` に `state`/`allClear`/`score`/`highScore` から完全に独立した `easterEgg = { active, timer }` フラグを新設する方式とし、演出専用の6秒タイマーが切れると自動的に消える。誤答・キャンセル時は何も起きず静かにダイアログが閉じるのみ(`js/configPanel.js`)。(2) README.md を新規作成し、富士急ハイランド「絶望要塞」へのリスペクトという開発動機、3ステージの構成、パドル操作、玉の物理挙動、Config画面、オールクリア演出、ハイスコア、デモプレイの各仕様をまとめた。末尾に隠しコマンドの手順をトリビア的にさりげなく記載。操作方法の項にスマートフォンのタッチ操作の方が快適である旨の一文を追加。(3) アイドル時のデモプレイ(アトラクトモード): `GAME.idleDemoDelaySec=60` を新設し、タイトル画面のまま60秒操作がないと `Game.startDemo()` が呼ばれ、1〜3のステージをランダムに1つ選んで専用の `this.demo`(独立した stageData/paddle/ball)でボールのx座標を追いかけるだけの簡易AIによる自動プレイを開始する(`state: 'demo'`)。実プレイの `stageData`/`ball`/`score`/`lives`/`timeRemaining` には一切触れないため記録に影響しない。タップ・クリック・キー入力のいずれでも即座にタイトルへ戻る(`js/main.js` のグローバル `pointerdown`/`keydown` リスナーで検知し `Game.stopDemo()` を呼ぶ。`PaddleInput` の `onTap` は矢印キー等を拾わないため専用リスナーを追加)。Node シミュレーション(デモAIの30秒連続実行・実記録が変化しないこと・イースターエッグが6秒で自動終了すること)と Playwright ヘッドレスブラウザ(正答/誤答/キャンセルの3パターン・実際に61秒待ってデモが自動開始し画面遷移すること・クリックで即終了すること)の両方で動作確認済み(検証用一時スクリプトはコミット前に削除)。
- `0.6.1`: 規定値(`js/config.js` の `BALL`)を空気抵抗 `drag: 0.2→0.4`、キャッチ速度しきい値 `catchSpeedThreshold: 110→100` に変更。ステージ2の下段2段(`js/stages/stage2.js`)を、6列連続配置から「左3列+中央1ブロック分の空き+右3列」の構成に変更(実装は7列ぶんの仮想グリッドを使い中央index=3のみ空けることで、通常の連結ギャップ+1ブロック分の空白+通常の連結ギャップという見た目を再現)。右側3列は元のチェッカー柄パターン(中央の空きがないと仮定した場合の6列連続パターン)を反転させ、ブロックとジャマーの位置を入れ替えた。drag上昇による全ステージのクリア可能性への影響を Node シミュレーションで再検証: 単発の弾道(壁反射なし)では最大到達高さが大きく下がる(直上発射で y≈43→146)ものの、実際のゲームプレイでは壁反射のたびに `ensureMinBounceSpeed` で速度が再補充されるため、左右の壁で跳ね返りながら上昇する軌道を使えばステージ1最上段(y=82)へ到達しブロックに命中できることを確認(例: x=35, 角度-94°で minY=16 まで到達)。ステージ2新配置・ステージ3(既に1.5倍幾何済み)も同条件下で命中を再確認し、3ステージすべて理論上クリア可能なままであることを検証した(検証用一時スクリプトはコミット前に削除)。
- `0.6.0`: ステージ2下段2段の通常/ジャマー混在ブロックを各段8列から6列に削減(`js/stages/stage2.js` の `cols`)。既存の中心揃え計算式(`rowW`/`startX` が `cols` に対して対称)により、列数を減らすだけで自動的にプレイフィールド中央に再配置される。段ごとのチェッカー柄反転ロジック(`isTarget`)は変更なし。ステージ3の回転リング3枚(`js/stages/stage3.js` の `ringDefs`)を、各リング独立に「新しい空き角度=現在の空き角度×1.5、新arcDeg=360/segments-新しい空き角度」の式で再計算(1枚目 42°→33°、2枚目 34°→28.5°、3枚目 28°→24°)。最内周リングの半径をコア外周(`CORE_RADIUS=24`)に内側輪郭がちょうど接触する `29`(=24+halfThickness 5)まで縮小し、リング間隔(半径差)を従来の26pxから1.5倍の39pxへ拡大した結果、3枚の半径は `29/68/107` になった。変更後、Node上で `stepBall()`/`updateMotion()` を直接呼び出すシミュレーション(発射位置7通り×発射角7通り=49パターン、パドルなしの純粋な物理挙動)でコアへの命中を確認(6/49パターンで命中、うち1パターンは複数回命中でコアを完全破壊)、続けて Playwright ヘッドレスブラウザでステージ3へジャンプし実際にプレイさせた結果、4秒以内にコアが2回ヒットされスコアが加算されることを目視確認し、穴拡大・間隔拡大後も理論上クリア可能であることを再検証した。Config画面(`js/configPanel.js`)に「規定値に戻す」ボタン(`#config-reset`)を追加し、押すと全パラメータ(パドル移動速度/重力/空気抵抗/制限時間/ボール数/キャッチ速度しきい値)が `config.js` の既定値に戻る機構を実装。各パラメータのラベル表示を `現在値 (既定値: X)` の形式に統一し、常に既定値との差が一目でわかるようにした(既定値はモジュール読み込み直後、ユーザー操作が発生する前の値を `defaultValue` として固定して使用)。
- `0.5.0`: 玉の物理モデルを等速反射から重力+空気抵抗(減衰)モデルへ刷新(`BALL.gravity`/`BALL.drag`/`BALL.launchSpeed`/`BALL.minBounceSpeed` を `js/config.js` に集約)。パドル速度ベクトルと玉速度ベクトルの内積で「受け」動作を判定し、反発係数を切り替える機構(`paddleRestitutionNormal`/`paddleRestitutionCatch`)を追加。減速後の速度が `BALL.catchSpeedThreshold` を下回るとパドルに吸着する玉のキャッチ・追従・再発射(初回発射と同一操作系に統一)を実装。HUD近くに Config ボタンを新設し、パドル移動速度・重力・空気抵抗・制限時間・ボール数・キャッチ速度しきい値をスライダーで、ステージ選択をセレクトボックスで実行時変更できる `js/configPanel.js` を追加(パネルを開いている間は `Game.paused` でゲーム・タイマーを一時停止)。1→2→3を通しでクリアした場合のみ `CONGRATULATIONS` 表示+花火演出(`js/fireworks.js`)によるオールクリア演出を発生させ、Configのステージ選択でジャンプした場合は対象外にする `sequentialClearEligible` フラグを追加。HUDにハイスコア(`js/highscore.js`、`localStorage` キー `tilt_high_score`)と残り時間を常時表示し、`SUBUTOMO_BADGE_CONFIG.privacy.localStorage` を `true` に変更してストレージ利用を明示。Node上で実際の `stepBall()`/`collidePaddle()` を用いたシミュレーションにより、ステージ1最上段・ステージ3最外周リングへの到達可能性、およびステージ2の凹ブラケット中央42px帯の重力下での通過可能性(狙えば通せる本物の隙間が残っていること)を検証し、既定係数(`launchSpeed=400, gravity=120, drag=0.2, minBounceSpeed=400`)のまま3ステージすべてがクリア可能であることを確認した(係数調整は不要だった)。
- `0.4.0`: 玉の速度ベクトルが水平から5度未満の角度になった場合、速さ(エネルギー)を変えずに角度成分だけ水平から5度になるよう補正する `clampHorizontalAngle()` を `js/physics.js` に追加。壁反射・パドル反射・ターゲット/ジャマー/回転リング反射(共通の `reflect()` 経由)のすべての角度変化タイミングで適用。ステージ3の中央コアの初回ヒット時ランダムHPを2桁(10〜99)から1桁(1〜9)に変更(`js/stages/stage3.js` の `randomHp`)。
- `0.3.0`: favicon を独自デザイン(開口リング+パドル+ボール)から Subutomo エコシステム共通の正本デザイン(`subutomo-template/assets/favicon.svg` 等、`C:\dev\` 配下の全サイトで共有される「オレンジ背景+白円+手描き風の『す』」)へ差し戻し、独自生成スクリプト `scripts/gen_favicon.py` は不要になったため削除。ステージ1の3列目にある左右対称の破壊不能ジャマーのうち最も外側の2つを、1列目と同じハードブロック(3ヒット・ドット密度濃いめ)に変更(内側寄りの2つは破壊不能ジャマーのまま維持)。ステージ2の凹ブラケットの脚を20px→14pxに短縮し、中央正方形横の穴の高さを玉1個分(12px)拡大(30px→42px)。
- `0.2.0`: GitHub Pages 公開に伴い `CNAME`(`tilt-breakout.subutomo.dev`)を追加し CLAUDE.md の公開URLを記入。favicon をプレースホルダの全円リングから「開口(ギャップ)リング+パドル+ボール」の本番デザインへ差し替え。パドル移動速度をポインタ/タッチ/キーボードの全入力方式で一律3倍化(`PADDLE.moveSpeedMultiplier`)。ステージ2の回転十字ジャマーを中央正方形からさらに玉1個分(12px)遠ざけ、クリアランスを23px→35pxに拡大。
- `0.1.0`: 初回実装。`docs/reference/stage-mock.html` を正として、3ステージ(クラシック配置/凹型ブラケット要塞/三重回転リング)・2Dパドル操作(傾き連動・スマホ2本指対応)・スコア/ライフ/ステージ進行の状態機械を実装。Subutomo 共通フッター/バッジ/manifest/favicon(プレースホルダ)を配線。

## 動作確認メモ
- ローカル `python -m http.server 8791` + Playwright ヘッドレス Chromium で title→serve→playing の一連の遷移、1本指ドラッグでのパドル移動、ブロック破壊によるスコア加算(0000→0020)を確認。
- コンソールエラーは Cloudflare Analytics のプレースホルダトークンによる CORS エラーのみ(実トークン登録後に解消する想定。コードのバグではない)。
- ステージ2・3は幾何定義をモックの数値からそのまま移植しており、目視での個別プレイ確認(要塞ステージの凹型ギャップ・三重リングの隙間)は未実施。**次回セッションでの優先確認事項。**
- `0.5.0`: Node上で実際の `js/physics.js` の `stepBall()` を用いた数値シミュレーションを実施(一時スクリプト、コミットには含めず検証後に削除)。
  - ステージ1最上段(y=82)・ステージ3最外周リング(y=83)への到達確認: パドル可動域下限(`yMax`)からの直上発射で最小到達高さ y≈43.2、発射角の全振れ幅でも最悪ケース y≈64.2 となり、いずれも両ステージの最上部より高く届くことを確認(OK)。
  - ステージ2の凹ブラケット中央42px帯の通過確認: 中央スクエア(的)に狙って当てる軌道(y=110中心、速度200/300/400px/s)はすべて脚(16px幅)に接触せず通過して的に到達することを確認。中央からのオフセット(±18px)や上下角を加えた63パターン中45パターンが到達、残りは脚に接触(=「狙えば必ず抜けられる本物の隙間」の要件は満たすが、無条件にどの角度でも通るわけではない設計として妥当と判断)。
  - パドルの「受け」判定: 内積が正(パドルが玉と同方向)のときのみキャッチが発動し、内積ゼロ・負(静止/迎え撃ち)では通常反発+最低速度保証(`minBounceSpeed=400`)が働くことを `collidePaddle()` の直接呼び出しで確認。
- `0.5.0`: Playwright ヘッドレス Chromium で以下を追加確認。Config パネルを開いている間は `TIME` 表示が変化しない(ゲーム一時停止)こと、ステージ選択でステージ3へジャンプすると `STAGE 3/3` に切り替わりパネルが自動で閉じてプレイ再開すること、ステージ3の三重回転リングが正しく描画されること。コンソールエラーは Cloudflare Analytics のプレースホルダトークンによるものだけで新規のJSエラーは発生していない。
- **未実施(次回以降の課題)**: 実ブラウザでの手動プレイによるキャッチ→再発射の触感確認、および1→2→3通しクリアでのオールクリア演出(花火・CONGRATULATIONS表示)の目視確認。自動化スクリプトでは玉の挙動が確率的でキャッチ/クリアの再現が難しく未実施。
- `0.6.0`: ステージ3の新リング幾何(穴1.5倍・最内周コア接触・間隔1.5倍)を Node シミュレーション(一時スクリプト、検証後削除)で検証。パドルを画面外に固定し発射位置7通り×発射角7通り=49パターンを最大120秒シミュレートした結果、6パターンでコア命中(うち1パターンはランダムHP=7に対し7回連続命中してコアを完全破壊)。Playwright ヘッドレスブラウザ(一時ドライバ、検証後削除)でも Config画面からステージ3へジャンプし、発射後4秒でスコアが `0000`→`0050`(コアヒット2回分)に増加することを確認し、穴・間隔拡大後も理論上クリア可能であることを実プレイに近い形で再検証した。Config画面の「規定値に戻す」機能も同様に Playwright で、重力スライダーを既定値120から300へ変更→ラベルが `300 (既定値: 120)` に変化→リセットボタン押下で `120 (既定値: 120)` に戻り入力欄の値も `120` に復元されることを確認した。

---

## このセッションでの判断ログ(ユーザー確認を省略し、最も妥当と判断して決定した事項)

1. **`config/subutomo-sites.json` 台帳への自エントリ追加を見送った。** qed-arcade からそのままコピーし、`tilt-breakout` の項目は追加していない。台帳の正本は `subutomo-template` 側にあるため、勝手に改変せず現状維持を選択。追加が必要なら別途判断すること。
2. **`scripts/drive.mjs`(Playwright 動作確認ドライバ)はコミット対象から除外し `.gitignore` に追加した。** このマシン固有の絶対パス(`C:/Users/subut/AppData/Local/npm-cache/_npx/...`)をハードコードしており、他環境では動かないため。`scripts/shots/`(スクリーンショット)も同様にローカル専用として除外。
3. ~~favicon/apple-touch-icon はプレースホルダ~~ → **`0.2.0` で本番デザインへ差し替え済み。** Pillow 非依存の自前 PNG エンコーダ(`scripts/gen_favicon.py`)は維持しつつ、意匠をステージ3の「回転リングの開口(ギャップ)」を象徴する開いたリング+パドル+ボールに変更(判断ログ14参照)。
4. **Cloudflare Web Analytics のトークンはプレースホルダ(`PLACEHOLDER_TOKEN_REGISTER_LATER`)のまま。** 実トークンは Cloudflare 側での登録が必要なため後日差し替え(PLAN.md フェーズ3に記載)。
5. **版定数名は `APP_VERSION` を採用。** ユーザー指示により qed-arcade の `VERSION` ではなく talk-prompter/asset-tl 系の命名に統一。`js/config.js` 単一箇所で管理。
6. **フィードバックフォームの `entry.2036331541` 値は `TILT BREAKOUT (tilt)` とした。** 「tilt-breakout であることが分かる相当の値」という指示に対する具体的な文字列の選定。
7. **フッターに `©` 行を書かない方針を厳守。** Subutomo バッジ(`subutomo-badge.js`)側が担当する仕様のため、`index.html` にはコピーライト行を含めていない。
8. **`.claude/settings.json` は今回限りでなく今後の通常セッションの標準設定として作成。** `defaultMode: "acceptEdits"` + `git push` 系コマンドのみ拒否。今回セッションの push 許可はこの設定とは別に、ユーザーからの明示的な一回限りの許可に基づく。
9. **ボール発射角の計算式は自己設計。** モックには発射角の仕様が明記されていなかったため、`launchAngleDeg`(基準 -68°)を中心に、パドル傾き `tilt / maxTiltDeg` に応じて左右に振る式を独自に採用(`game.js` の `_launchFromPaddle`)。常に上方向へ飛ぶことを優先。
10. **衝突解決は1フレームにつき1イベントのみ処理する方式を採用。** 複数形状に同時衝突した場合の処理を単純化し、安定性を優先(貫通・多重ヒットのバグを避けるための設計判断)。
11. **ステージ2の回転十字ジャマーと中央スクエアの最小クリアランス(≥20px)は、アーム最大伸長時の半径とブラケットギャップ位置の幾何計算で確保されるよう定数を設定。** モックの数値(`cross jammer x:∓75, size:34` 等)をそのまま採用しつつ、「クリア不能な配置を作らない」という最重要原則に沿って値を検証済み(詳細な再検証はフェーズ3以降の目視確認で継続)。
12. **ステージ3の中央コアHPは初回ヒット時に 10〜99 のランダム2桁整数を確定させ、以後固定する方式。** 「?」表示から実数値表示への切り替えタイミングをヒット時点と解釈。
13. **README は今回作成していない。** ユーザー指示は CLAUDE.md/STATUS.md/PLAN.md の3点セットのみだったため、README の新規作成は範囲外と判断し見送った。
14. ~~(0.2.0) favicon の意匠を「全円リング」から「開口(ギャップ)リング」へ変更~~ → **`0.3.0` で撤回。** 独自デザインは Subutomo エコシステムの統一ブランドと乖離していたため、ユーザー指示によりロールバック(判断ログ17参照)。
15. **(0.2.0) パドル速度3倍化は、ポインタ/タッチのドラッグ量にも一律適用。** 「パドルのスピードを3倍に」という指示はキーボードのみとも読めたが、入力方式ごとに体感速度が食い違わないよう `PADDLE.moveSpeedMultiplier` を新設し、ドラッグ量(dx/dy)とキーボード基準速度の両方に同じ倍率を掛ける方式に統一した。
16. **(0.2.0) 「玉一つ分」はボール直径(`BALL.radius*2`=12px)と解釈。** ステージ2の回転十字ジャマーのオフセットに +12px を加算(75→87)。半径(6px)ではなく直径で解釈した方が「玉一個分の実体の大きさ」により近いと判断。
17. **(0.3.0) favicon の正本は `C:\dev\subutomo-template\assets\favicon.svg` / `favicon-32.png` / `favicon-180.png` と特定。** `C:\dev\` 配下の全サイト(qed-arcade / talk-prompter / mnym-lab / subuta-site 等)で `favicon.svg` の md5 が完全一致していることを確認し、`subutomo-template`(テンプレートリポジトリ)を正本源として採用。独自解釈を加えず `cp` でバイト単位そのまま複製(SVGに埋め込まれたbase64画像データを含むため、Edit/Writeでの再構成はせず、バイナリ扱いで`cp`を使用。CLAUDE.mdの「バイナリ/非日本語ファイルのコピーのみBash cp可」規定に該当)。
18. **(0.3.0) `scripts/gen_favicon.py` を削除。** favicon を正本コピーへ切り替えたため、独自生成スクリプトは不要になった(使われないコードを残さない方針)。
19. **(0.3.0) ステージ1「最も外側」の解釈。** 3列目の対称パターン(ジャマー/ターゲットが `bucket = floor(|c-3.5|)` で決まる)のうち、`bucket` が最大値(=3、両端 c=0,7)の2つを「最も外側」と解釈してハードブロック化。`bucket=1`(c=2,5)の内側寄りジャマーは指示通り維持。
20. **(0.4.0) 「水平から5度」の数学的定義。** 水平からの角度を `asin(|vy| / speed)` で定義し、これが 5°(`(5*Math.PI)/180` rad)未満になった場合のみ、`vx = sign(vx) * speed * cos(5°)`、`vy = sign(vy) * speed * sin(5°)` に再計算する方式を採用(`speed = hypot(vx, vy)` を保存してから角度のみ差し替えるため、速さ・エネルギーは厳密に不変)。`vx`または`vy`がちょうど0の場合の符号は、正(右向き/下向き)をデフォルトとした(ゼロ除算・不定符号の回避)。
21. **(0.4.0) 「あらゆるタイミングでこの制約を適用」の実装箇所。** フレーム末尾に一括で1回だけ補正する方式ではなく、角度が変化しうる箇所それぞれに個別に `clampHorizontalAngle()` の呼び出しを追加した: 共通の `reflect()`(ターゲット/ジャマー/回転リングの反射すべてがここを通る)、`collidePaddle()`(パドル反射)、`stepBall()` 内の壁反射ブロック直後、の3箇所。指示文の「あらゆるタイミングで」という文言をより文字通り満たすため、フレーム単位の一括処理より粒度を細かくする設計を選択した。
22. **(0.4.0) 「2面中央ブロックの乱数を1桁に」の対象をステージ3の中央コアと判断。** コード確認の結果、「初回ヒットで『?』表示から乱数の耐久値が確定する」機構(`revealOnHit` + `randomHp`)は2面の中央正方形(固定 `hp:24`、乱数フィールドなし)には存在せず、3面の中央コア(`randomHp:[10,99]`)にのみ存在することを確認した。ゲーム内で唯一該当する箇所であるため、ユーザーが言う「2面」はステージ番号の言い間違い/勘違いと判断し、3面の中央コアの `randomHp` を `[10, 99]` から `[1, 9]` に変更した。
23. **(0.5.0) 重力・空気抵抗の既定係数を `launchSpeed=400, gravity=120, drag=0.2, minBounceSpeed=400` に決定。** まず簡易な放物運動の公式でNode上の数値計算を行い、単純上昇の最大到達高さが約464.5pxとなる組み合わせを探索。続けて実装した `stepBall()`/`collidePaddle()` を直接呼び出す Node シミュレーションで、パドル可動域下限からの発射・全反射直後の最低速度保証を経ても3ステージすべての最上部(ステージ1最上段/ステージ3最外周リング)に到達できること、およびステージ2の凹ブラケット中央帯を狙えば通過できることを実測で確認した。係数の追加調整は不要と判断。
24. **(0.5.0) パドルの「受け」判定はパドル速度ベクトルと玉速度ベクトルの内積の符号のみで判定する方式を採用。** 内積が正(同方向=追いかけている)ならキャッチ用の低い反発係数 `paddleRestitutionCatch`、内積がゼロまたは負(静止/迎え撃ち)なら通常反発係数 `paddleRestitutionNormal` を適用し、通常反発後は必ず `minBounceSpeed` 以上を保証することで、意図せぬ低速化によるクリア不能化を防いだ。
25. **(0.5.0) キャッチ中もステージのジャマー/回転リングのアニメーションと制限時間は止めない一方、玉自体の物理更新(`stepBall`)だけをスキップする設計にした。** 「キャッチ中もタイマーは進行し続ける」という指示を文字通り解釈しつつ、キャッチ中は玉がパドルに完全追従する必要があるため、玉の位置更新だけは `game.js` 側でパドル追従処理に差し替えた。
26. **(0.5.0) オールクリア判定は「1→2→3を1度も面選択でジャンプせずに通しでクリアしたか」を `sequentialClearEligible` フラグ(既定 `true`)で管理し、Config画面の `jumpToStage()` を呼んだ時点で `false` に固定する方式にした。** ジャンプ後に1面からやり直しても復活しない(その周回はオールクリア対象外のまま)仕様とした。「面を飛ばした場合は対象外」という指示を、その周回全体に適用されるものと解釈。
27. **(0.5.0) Config画面でパドル移動速度/重力/空気抵抗/キャッチしきい値を変更した場合は即座にプレイに反映され、制限時間・ボール数(残機)の変更は次回のラン開始時(タイトル/ゲームオーバーからの再スタート、または面ジャンプ時)から反映される設計にした。** 前者は毎フレーム `config.js` の値を直接参照する既存の実装に自然に乗せられる一方、後者(`timeRemaining`/`lives` の初期値)はラン開始時に一度だけ設定される値のため、プレイ中に変更してもその場で即時反映すると挙動が不安定になる(残り時間が急に増減する等)と判断し、次回開始時点での反映に統一した。
28. **(0.5.0) ハイスコアの永続化キーは `tilt_high_score`、データ形式は `{score, date}` とした。** qed-arcade の `qed_ranking_data`(複数ランキングを保持する配列)は tilt-breakout の「単一のハイスコアのみ」という要件に対して過剰なため、同じ「try/catchで失敗を握りつぶす」という設計思想を踏襲しつつ、単一値用にシンプル化した独自キー・独自データ形式を採用した。
29. **(0.5.0) `js/game.js` の状態遷移で、`gameOver` からのタップは即座に新しいランを開始(`_startRun()`)する一方、`win` からのタップはタイトル画面(`title`)に戻るのみとした。** 既存の HUD メッセージ文言(`gameOver`: 「TAP でリトライ」、`win`: 「TAP でタイトルへ」)が実装前から存在しており、その文言が示す遷移仕様にコードを合わせた。
30. **(0.6.0) ステージ2「左端2列を削除して6列に」の実装は列数削減のみで対応した。** 既存の中心揃え計算式(`rowW = cols*bw+(cols-1)*gap`、`startX = -rowW/2+bw/2`)は `cols` に対して対称なため、「元の8列のうち左端2列を削除してから中央に寄せる」のと「最初から6列で中央に配置する」のは最終的な見た目・座標が数学的に完全一致する。どの元列が生き残ったかを追跡する特別な処理は不要と判断し、単純に `cols: 8→6` の変更のみを行った。
31. **(0.6.0) ステージ3のリング半径再計算で「間隔」は隣接するリング半径の差(等差)と解釈した。** 元の半径 `50/76/102` の間隔は一律26pxだったため、これを1.5倍した39pxを新しい間隔として採用し、「コアに接する最内周(半径29)」を基準に `29, 29+39=68, 68+39=107` と積み上げる方式にした。ユーザー指示の「そこから1.5倍の間隔で外側2枚を配置する」という表現と、間隔=リング半径の差という物理的に自然な解釈が一致するため、他の解釈(例: 内側と外側の間隔を独立に1.5倍する等)は採らなかった。
32. **(0.6.0) Config画面の「既定値」は `config.js` のモジュール読み込み直後(=ユーザー操作が発生する前)に `get()` を呼んだ結果をその場で固定し、以後は再取得しない方式にした。** `config.js` の定数オブジェクト(`PADDLE`/`BALL`/`GAME`)自体はミュータブルで実行時に書き換えられるため、もし「既定値」を都度 `config.js` から再取得する設計にすると、ユーザーがスライダーを動かした瞬間に「既定値」表示自体も変わってしまい意味をなさなくなる。そのため `js/configPanel.js` のモジュール評価タイミング(＝ページ読み込み時点、まだ誰も値を変更していない瞬間)でスナップショットを取る設計とした。
33. **(0.6.1) 「下二段のブロック中央に1ブロック分の空きを作り左右6個ずつにわける」を、各段(2段)ごとに「左3列+中央1ブロック分の空き+右3列」(左右合わせて6個/段、2段で12個)と解釈した。** 元の6列連続配置の中央(3列目と4列目の間)を分割する形とすることで、列幅(`bw=34`)や総ブロック数を変えずにプレイフィールド内に収まる(列数を12列に増やす解釈だと必要幅514pxとなり400pxのキャンバス幅を超えるため不採用)。実装は7列ぶんの仮想グリッドで中央(index=3)のみ空ける方式とし、空きの物理的な幅(隣接ブロックの端から端まで)がちょうど「通常の連結ギャップ+1ブロック分+通常の連結ギャップ」(=46px)になるようにした。「1ブロック分の空き」を通常の隙間(6px)ではなく1ブロックが丸ごと収まる空間と解釈した。
34. **(0.6.1) 「右側の6個のブロックとジャマーの位置を入れ替える」は、中央の空きがないと仮定した場合の6列連続チェッカー柄パターン(元の実装のまま)を基準とし、右側3列(元インデックス3,4,5相当)のみそのターゲット/ジャマー判定を反転させる方式で実装した。** 左側3列は基準パターンをそのまま維持し、右側3列だけ `isTarget = !isTarget` で反転させることで、指示の「右側の」という限定を字義通りに満たした(結果的に今回のブロック数・チェッカー周期の組み合わせでは、左右の見た目パターンが偶然一致するが、これは反転処理を経た結果であり無反転の状態とは異なる)。
36. **(0.7.0) 隠しコマンドとデモプレイの両方とも、既存の `state`/`score`/`highScore`/`allClear` などの実記録用フィールドを一切書き換えず、専用の独立したフィールド(`easterEgg`/`demo`)だけで完結させる設計にした。** 「実際の記録に一切影響を与えない」という指示を、個別に条件分岐で除外するのではなく、そもそも実記録側のコードパスに触れさせない構造にすることで確実に満たす方針を採った。
37. **(0.7.0) 隠しコマンドの正誤判定・演出終了は、タップされても実際の状態遷移(`handleTap()` の本来の分岐)に波及させないようにした。** 演出中にユーザーがタップした場合、`easterEgg.active` を最優先でチェックして演出を止めるだけで `return` し、`title`→`_startRun()` 等の本来のタップ処理は行わない設計とした。「演出のみの再現」という要件上、演出中の操作が実ゲームの開始に紛れ込むのは望ましくないと判断。
38. **(0.7.0) デモプレイの終了検知は、`PaddleInput` の `onTap`(Space/Enter・単一ポインタのみ)に頼らず、`js/main.js` に別途グローバルな `pointerdown`/`keydown` リスナーを追加する方式にした。** 「タップ・クリック・キー入力のいずれでも終了」という指示に対し、矢印キーやQ/E等の移動系キー入力は `onTap` を発火しないため、既存の入力処理に手を入れず独立したリスナーを足す方が安全かつ既存の入力ロジックへの副作用がないと判断した。
39. **(0.7.0) デモの自動開始条件を「`game.state === 'title'` かつ `game.paused === false`(Configパネルが閉じている)」の間だけアイドル秒数を加算する方式にした。** Configパネルを開いたままアイドル秒数が積み上がってパネル使用中にデモへ切り替わる事態を避けるための判断(ユーザー指示にはなかったが、UXとして不自然なため自主的に対応)。
40. **(0.7.0) README.md を新規作成した(判断ログ13を撤回)。** 以前のセッションでは「3点セット(CLAUDE.md/STATUS.md/PLAN.md)のみが指示範囲」と判断し見送っていたが、今回ユーザーから明示的にREADME.md新規作成の指示があったため作成した。内容はSTATUS.md/PLAN.mdの変更履歴を参照し、実装済み仕様を漏れなく反映した。
41. **(0.6.1) 空気抵抗を0.2→0.4に上げたことによる全ステージのクリア可能性への影響を再検証した。** 単発の弾道(壁反射なし)のみで見ると最大到達高さが大きく下がる(直上発射で y≈43→146)ためステージ1最上段(y=82)に届かなくなるように見えるが、`stepBall()` は壁反射のたびに `ensureMinBounceSpeed()` で速度をリセットする仕様のため、左右の壁で跳ね返りながら斜めに上昇する軌道を使えば実際には十分な高さまで到達できることをNodeシミュレーションで確認した(例: 発射位置x=35・角度-94°で minY=16 まで到達しブロックに命中)。ステージ2の新配置・ステージ3(既存の1.5倍幾何)についても同条件下でコア/ブロックへの命中を再確認済み。ユーザーからの明示的な再検証依頼はなかったが、CLAUDE.mdの「理論上100%クリア可能」の原則が物理定数変更全般に及ぶと判断し、自主的に実施した。
42. **(0.7.1) デモプレイの縦方向AIは「待機中は下限付近で緩やかに上下へ揺れ、ボールが水平方向に近くかつ落下中のときだけ迎えに上がる」という条件分岐にした。** 常にボールのy座標を直接追わせる単純な実装も可能だったが、それだとボールが遠くにあってもパドルが不自然に上下へ張り付き続け、実プレイのパドル操作(基本は下限付近で構え、必要なときだけ動く)との見た目の乖離が大きいと判断。傾き(`tilt`)は横移動速度に比例させ、`physics.js` の `collidePaddle()` が実際に `paddle.vy`/`tilt` をキャッチ判定・反射角へ使う実装であることを確認した上で追加したため、見た目のアクセントに留まらず当たり判定にも反映される。ユーザー指示は「縦の動きや傾きも入れて」という簡潔なものだったため、具体的な挙動は自主的に設計した。
43. **(0.7.1) 隠しコマンドの花火演出を「時間で終わらせず操作されるまで継続」に変更するにあたり、本番のオールクリア演出(`state==='win' && allClear`)側は変更不要と判断した。** 本番演出はもともと固定タイマーを持たず `state` が `win` のままである限り `fireworks.update()` が呼ばれ続ける実装だったため、「本番も短くて寂しいなら直す」というユーザーの条件付き指示に対し、本番側は既に「操作(タップで `win`→`title`)まで継続」の要件を満たしていたと確認し、変更対象を打ち上げ頻度(`FIREWORKS_BURST_RATE`)の引き上げのみに絞った。
