# PLAN.md — 実装計画

## 目標
回転リング×固定障害物×ブロック崩しを組み合わせたブラウザゲームを、モック(`docs/reference/stage-mock.html`)の見た目・配置を踏襲しつつ、保守可能なモジュール構成で実装する。

## フェーズ構成

### フェーズ 1: セットアップ ✅
- [x] リポジトリ作成・3点セット雛形
- [x] サイト共通部品の配線(バッジ/フッター/manifest/favicon)
- [x] `js/config.js` に `APP_VERSION` を単一の源として設置

### フェーズ 2: コアゲーム実装 ✅
- [x] 数学ユーティリティ・Ball エンティティ
- [x] 物理・当たり判定(矩形/円/十字/三角/回転リング)
- [x] ステージ1〜3のデータ定義(モック準拠)
- [x] パドル入力(2Dドラッグ+傾き、マウス/タッチ/キーボード)
- [x] レンダラー(フレーム装飾・ドットパターン・半透明ジャマー)
- [x] 状態機械・スコア・ライフ管理
- [x] ローカルサーバー+Playwright での動作確認(タイトル→発射→プレイ→反射)

### フェーズ 2.5: 玉の物理刷新・キャッチ・Config画面・オールクリア・ハイスコア ✅
- [x] 重力+空気抵抗モデルへの刷新(`js/physics.js`)。係数はNode実測シミュレーションで検証済み
- [x] パドルの「受け」動作(内積判定による反発係数切り替え)
- [x] 玉のキャッチ・パドル追従・再発射の統一実装
- [x] Config画面(パドル速度/重力/空気抵抗/制限時間/ボール数/キャッチしきい値/ステージ選択)
- [x] オールクリア判定(通しクリアのみ対象)と花火演出
- [x] HUDへのハイスコア(localStorage永続化)・残り時間の常時表示

### フェーズ 3: 公開・運用
- [x] GitHub リポジトリ作成・初回コミット・push
- [x] GitHub Pages 有効化(`https://tilt-breakout.subutomo.dev/`)・公開URLを CLAUDE.md に記入・カスタムドメイン用 `CNAME` を追加
- [x] favicon/apple-touch-icon を Subutomo エコシステム共通の正本デザイン(オレンジ背景+白円+「す」)へ差し替え
- [ ] Cloudflare Web Analytics の実トークンを登録し、`index.html` のプレースホルダを差し替える
- [ ] `config/subutomo-sites.json` 台帳(正本 subutomo-template 側)への `tilt-breakout` エントリ追加を検討する

### フェーズ 4: 今後のオリジナルステージ拡張(軽量ロードマップ)
- [ ] ステージ4以降の設計方針を決める(例: 複数の凹型ブラケットを非対称配置、リングの段数を増やす等)
- [ ] 新ステージ追加時も「理論上100%クリア可能」の幾何検証を PLAN.md か個別 Issue に残す
- [ ] 難易度カーブ(ボール速度・ジャマー角速度)の調整方針を検討
- [ ] サウンド/エフェクトの追加要否を検討(現状は無音・視覚のみ)

## 未解決の課題
- Cloudflare Analytics トークン未登録(プレースホルダ運用中)

## 決定事項
| 日付 | 決定内容 | 理由 |
|------|----------|------|
| 2026-08-22 | tilt-breakout を新規プロジェクトとして開始 | ユーザー指示(一括実行・確認最小化) |
| 2026-08-22 | ステージ2/3の物理定数はモックの数値をそのまま採用 | モックが配置・幾何関係の正であるため踏襲 |
| 2026-08-22 | 版定数名は `APP_VERSION`(qed-arcade の `VERSION` ではない) | ユーザー指示により talk-prompter/asset-tl 系命名に統一 |
| 2026-08-22 | GitHub Pages を `https://tilt-breakout.subutomo.dev/` で有効化(ユーザー側で設定済み)。`CNAME` を追加し CLAUDE.md の公開URLを更新 | ユーザー指示 |
| 2026-08-22 | favicon を「リングの開口(ギャップ)+ 傾いたパドル + ボール」の本番デザインへ差し替え。ステージ3の回転リングのギャップ機構をより直接的に象徴する意匠に変更 | ユーザー指示(本番用faviconへの差し替え依頼)。プレースホルダの全円リングから、ゲームの核心メカニクスをより表す開口リングへ変更する自己判断 |
| 2026-08-22 | パドル移動速度を全入力方式(ポインタ/タッチ/キーボード)で一律3倍化。`PADDLE.moveSpeedMultiplier=3` を新設し、ドラッグ量・キーボード基準速度の両方に掛ける方式に統一 | ユーザー指示。入力方式ごとに個別の係数を持たせず単一の倍率定数に集約(config.js 単一の源の原則を維持) |
| 2026-08-22 | ステージ2の回転十字ジャマーのオフセットを中央正方形からさらに玉1個分(直径 `BALL.radius*2`=12px)遠ざけた(75→87) | ユーザー指示。クリアランス計算上は従来23px→35pxに拡大し、100%クリア可能の原則をより余裕を持って満たす |
| 2026-08-22 | favicon を独自デザインから `subutomo-template` の正本(オレンジ背景+白円+「す」)へ差し戻し、`scripts/gen_favicon.py` を削除 | ユーザー指示。Subutomoエコシステム全体で共有される統一ブランドを優先 |
| 2026-08-22 | ステージ1・3列目の左右最外側(bucket最大値)の破壊不能ジャマーをハードブロック(3ヒット)化し、内側寄り2つは維持 | ユーザー指示 |
| 2026-08-22 | ステージ2の凹ブラケットの脚長を20px→14pxに短縮し、中央穴の高さを30px→42pxに拡大 | ユーザー指示(玉1個分=12px広げる) |
| 2026-08-22 | 玉の速度ベクトルが水平から5度未満になった場合、速さを変えず角度成分のみ5度に補正する `clampHorizontalAngle()` を新設し、壁反射・パドル反射・`reflect()`(ターゲット/ジャマー/回転リング共通)の各変更箇所すべてに適用 | ユーザー指示(水平に近い角度での間延び防止) |
| 2026-08-22 | 「中央ブロックの乱数を1桁に」の対象を、ユーザー表記の「2面」ではなくステージ3の中央コア(`randomHp`)と判断し変更を適用(2面の中央正方形は固定HPで乱数機構自体が存在しないため) | コード確認の結果、該当機構が3面の中央コアにしか存在しないと判断(判断ログ22参照) |
| 2026-08-22 | 玉の物理を重力+空気抵抗モデルへ刷新し、既定係数 `launchSpeed=400, gravity=120, drag=0.2, minBounceSpeed=400` を採用 | Node上で実際の `stepBall()`/`collidePaddle()` を用いたシミュレーションにより3ステージすべての到達可能性・通過可能性を実測で確認済み(判断ログ23参照) |
| 2026-08-22 | ステージ2下段2段を各8列→6列に削減。既存の対称な中心揃え計算式のため列数削減のみで自動的に中央配置になる | ユーザー指示(左端2列削除・中央揃え)。STATUS.md 判断ログ30参照 |
| 2026-08-22 | ステージ3の回転リングを「穴1.5倍・最内周がコアに接触(半径29)・リング間隔1.5倍(半径29/68/107)」に再計算 | ユーザー指示。Node シミュレーション+Playwright ヘッドレスブラウザで再検証しクリア可能性を確認済み(STATUS.md 判断ログ31参照) |
| 2026-08-22 | Config画面に「規定値に戻す」ボタンと各パラメータの既定値常時表示を追加 | ユーザー指示。既定値はモジュール読み込み直後の値をスナップショットして使用(STATUS.md 判断ログ32参照) |
| 2026-08-23 | 規定値を空気抵抗0.2→0.4、キャッチ速度しきい値110→100に変更 | ユーザー指示。drag上昇後もNodeシミュレーションで3ステージすべてのクリア可能性を再検証済み(STATUS.md 判断ログ35参照) |
| 2026-08-23 | ステージ2下段2段を「左3列+中央1ブロック分の空き+右3列」構成に変更し、右側3列はブロック/ジャマーの位置を入れ替え | ユーザー指示。7列仮想グリッドで中央のみ空ける方式を採用(STATUS.md 判断ログ33・34参照) |
| 2026-08-23 | Config画面に隠しコマンド(メンテナンスボタン→合言葉「はだかのブタがいる」→「subuta」で既存の花火+CONGRATULATIONS演出のみ再現)を追加 | ユーザー指示。`state`/`score`/`highScore`/`allClear` に触れない独立フラグ `easterEgg` で実現(STATUS.md 判断ログ36・37参照) |
| 2026-08-23 | タイトル画面60秒無操作で自動デモプレイ(アトラクトモード)を開始する機能を追加 | ユーザー指示。実プレイと完全に独立した `demo` オブジェクトとランダムなステージ選択+簡易追従AIで実現。何らかの操作で即終了(STATUS.md 判断ログ38・39参照) |
| 2026-08-23 | README.md を新規作成(判断ログ13を撤回) | ユーザー指示による明示的な作成依頼。開発動機・全仕様・隠しコマンドのさりげない記載を含む(STATUS.md 判断ログ40参照) |
| 2026-08-23 | メンテナンスボタンの常時可視化・隠しコマンド花火の操作待ち継続化(+密度アップ)・デモプレイの縦移動/傾き追加、の3点を修正 | ユーザー指示。opacityトグルによる視認性バグの修正、固定タイマー廃止による本番演出との挙動統一、AIの縦方向・傾き挙動の追加(STATUS.md 判断ログ42・43参照) |
| 2026-08-23 | 画面上部にゲームタイトル見出しを追加・デモプレイの「DEMO PLAY」表示を削除・デモのステージクリア後に面が切り替わらないバグを修正しつつ切り替え時は直前と別の面を選ぶよう変更・傾きにランダムなゆらぎを追加 | ユーザー指示。調査の結果、面が切り替わらない事象は `startDemo()` の状態ガード漏れによる実バグと判明したため修正(STATUS.md 判断ログ44・45参照) |
| 2026-08-23 | フッター Feedback リンクのURLパラメータを更新。README.md の冒頭説明文・重複する操作性の一文・オールクリア演出の説明・小ネタをユーザー指定の文面へ差し替え | ユーザー指示。テキストのみの変更のためPATCHバンプ(STATUS.md 判断ログ46参照) |
